import { Request,Response } from "express";

function getGoodList(req:Request,res:Response) {
  const goodList:Api.GoodItem[] = [];
  for (let i = 1; i < 20; i++){
    goodList.push({
      id: i,
      status: i %3,
      name: 'name' + i
    })
  }
  const result = {
    data: goodList,
    success: true,
    total: 100,
    pageSize: '20',
    current: 1
  };
  res.json(result);
}

export default {
  '/api/goods/list': getGoodList
}